/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['maker.macks.gg','ultradroptest.s3.amazonaws.com'],
  }
}

module.exports = nextConfig
 